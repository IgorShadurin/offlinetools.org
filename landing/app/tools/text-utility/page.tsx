import React from "react";
import TextUtility from "./TextUtility";

export const metadata = {
  title: "Text Utility",
  description: "Transform text with line break conversion, case conversion, and line sorting utilities for efficient text processing.",
};

export default function TextUtilityPage() {
  return <TextUtility />;
}
